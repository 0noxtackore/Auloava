name: Deploy to Alibaba Cloud ACK

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and push image
        run: |
          echo "Build and push a container image to Alibaba Cloud Container Registry"
      - name: Deploy to ACK
        run: |
          echo "Use aliyun/ack-setup or kubectl with the ${{ secrets.ACK_KUBECONFIG }} secret to deploy to Alibaba Cloud Container Service for Kubernetes (ACK)."
