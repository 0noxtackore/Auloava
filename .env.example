# URL base del backend real (cuando lo tengas). Por defecto usamos el mock interno.
VITE_API_URL=http://localhost:3000/api

# Activa/desactiva el mock interno (true por defecto para que funcione sin backend).
VITE_MOCK_API=true
